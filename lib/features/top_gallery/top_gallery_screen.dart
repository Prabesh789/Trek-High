import 'package:flutter/material.dart';

class TopGalleryScreen extends StatelessWidget {
  const TopGalleryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
